import {NativeModules, Platform} from 'react-native';

const LINKING_ERROR =
  '$MODULE_NAME_RAW from generated package $NPM_NAME_RAW is not available. Backend: Native module (Kotlin/Java). Validate the generated dependency link and module integration, then rebuild with the existing plugin workflow.';
const $MODULE_NAME_RAW =
  Platform.OS === 'android' ? NativeModules.$MODULE_NAME_RAW : null;
if (!$MODULE_NAME_RAW) {
  throw new Error(LINKING_ERROR);
}
export default $MODULE_NAME_RAW;
