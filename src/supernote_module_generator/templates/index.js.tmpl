import {NativeModules, Platform} from 'react-native';

const LINKING_ERROR =
  '$MODULE_NAME_RAW from package $NPM_NAME_RAW is not available. Backend: Native module (Kotlin/Java). Install the local npm/Yarn dependency, then rebuild and redeploy the Supernote plugin. Check ReactNativeJS in logcat.';
const $MODULE_NAME_RAW =
  Platform.OS === 'android' ? NativeModules.$MODULE_NAME_RAW : null;
if (!$MODULE_NAME_RAW) {
  throw new Error(LINKING_ERROR);
}
export default $MODULE_NAME_RAW;
