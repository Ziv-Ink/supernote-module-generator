# $NPM_NAME_RAW

Generator `$GENERATOR_VERSION` · synchronous JSI C/C++ module · CLI type:
`JSI Module` · Calls are synchronous; do not use `await`

**Update replaces this README. Remove deletes the complete module. Commit first.**

## Edit C or C++

```text
android/src/main/cpp/
```

```cpp
// @SupernoteExport
double add(double left, double right) {
  return left + right;
}
```

## Call it

```typescript
import $MODULE_NAME_RAW from '$NPM_NAME_RAW';

const total = $MODULE_NAME_RAW.add(20, 22);
```

**JSI runtime requirement:** The generated library may compile but still be
blocked by PluginHost or SELinux. Verify the target environment before relying
on it. Do not use JSI for blocking or long-running work.

```bash
supernote-module validate $NPM_NAME_RAW --build
```

Add `--verbose` when diagnosing a failed build.

More help: [JSI modules](https://github.com/Ziv-Ink/supernote-module-generator/wiki/JSI-Modules) · [Update and Remove](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Managing-Modules)
