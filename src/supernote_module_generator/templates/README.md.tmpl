# $NPM_NAME_RAW

Generated by Supernote Module Generator `$GENERATOR_VERSION` as a **Kotlin/Java
Native Module** inside the parent plugin. This package is not a separate plugin.

**`update` replaces this README. Keep project notes in the parent plugin.**

## Names and call model

| Name | Value |
| --- | --- |
| Package/import | `$NPM_NAME_RAW` |
| JavaScript module | `$MODULE_NAME_RAW` |
| Android namespace | `$NAMESPACE` |
| Returned values | JavaScript Promise (`await`) |

## Edit Kotlin or Java here

User-owned source root:

```text
android/src/main/java/$PACKAGE_PATH/
```

`Example.kt` is live starter code. Edit or delete its example exports.

```kotlin
package $NAMESPACE

import $ANNOTATION_FQCN

class Example {
  @SupernoteExport
  fun add(left: Double, right: Double): Double = left + right
}
```

Call the default export from the parent plugin:

```typescript
import $MODULE_NAME_RAW from '$NPM_NAME_RAW';

const total = await $MODULE_NAME_RAW.add(20, 22);
```

Supported boundary values are `Boolean`/`boolean`, `Double`/`double`, `String`,
and `Unit`/`void` returns. Exported classes need a public constructor taking
`ReactApplicationContext`, `android.content.Context`, or no arguments.

## Ownership and lifecycle warning

Update preserves Kotlin/Java implementation below `android/src/main/java/`
(except generated packages) and replaces generator infrastructure, including:

- `.supernote-module.json`, package files, `index.js`, `index.d.ts`, and this
  README;
- `android/build.gradle.kts`, `android/autolink/`, the manifest, annotation,
  processor, and Android/KSP build output;
- the marked registration block in the parent `android/settings.gradle`.

Do not hand-edit replaceable files. Update has no dry-run or file-level diff.
**Remove deletes the entire module, including this user source. Commit first.**

## Regenerate and validate

From the existing plugin root, regenerate declarations and compile-check this
module's generated integration:

```bash
supernote-module validate $NPM_NAME_RAW --build --verbose
```

After validation, continue with the plugin workflow you already use.

Canonical guidance:

- [Add a module](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Add-a-Module)
- [Kotlin and Java modules](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Kotlin-and-Java-Modules)
- [Export functions and supported types](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Export-Functions)
- [Update and Remove safety](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Managing-Modules)
- [Troubleshoot generated modules](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Troubleshooting)
