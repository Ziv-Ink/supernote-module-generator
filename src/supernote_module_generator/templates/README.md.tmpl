# $NPM_NAME_RAW

Generator `$GENERATOR_VERSION` · Kotlin/Java module · CLI type:
`Native Module` · Returned values use Promises (`await`)

## Edit code here

```text
android/src/main/java/$PACKAGE_PATH/
```

```kotlin
package $NAMESPACE

import $ANNOTATION_FQCN

class Example {
  @SupernotePluginExport
  fun add(left: Double, right: Double): Double = left + right
}
```

## Call it from your plugin

```typescript
import $MODULE_NAME_RAW from '$NPM_NAME_RAW';

const total = await $MODULE_NAME_RAW.add(20, 22);
```

```bash
supernote-module validate $NPM_NAME_RAW --build
```

Add `--verbose` if validation fails.

## Before Update or Remove

Update replaces this README and generated files, but preserves the implementation
folder above. Remove deletes the complete module. Commit first.

More help: [Kotlin and Java modules](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Kotlin-and-Java-Modules) · [Update and Remove](https://github.com/Ziv-Ink/supernote-module-generator/wiki/Managing-Modules)
